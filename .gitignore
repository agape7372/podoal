node_modules
.next
out
*.tsbuildinfo
next-env.d.ts
prisma/*.db
prisma/*.db-journal
prisma/**/*.db
prisma/**/*.db-journal
# Env files — ignore ALL variants (.env, .env.production, .env.local, …)
# but keep the placeholder template tracked.
.env*
!.env.example
.DS_Store
Thumbs.db
*.log
*.tmp
.cache
*.png
!public/**/*.png
# Next OG 파일 컨벤션(src/app/opengraph-image.png)은 추적 — 스크린샷 위생 규칙의 유일한 소스 예외
!src/app/opengraph-image.png
take-screenshot*.mjs
# Local-only review/screenshot artifacts (never ship/track)
.shots/
taste-review/
.playwright-mcp/
# AI 아트 인테이크 원본(대용량) — PROMPTS.md만 추적, 산출물은 public/illustrations/로
art-intake/*.webp
art-intake/*.jpg
art-intake/*.jpeg

# Claude Code local tooling
.claude/

.vercel
art-intake/shots/
