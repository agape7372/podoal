상태: 완료 (2026-07-06 커밋 6543892 — 사용자가 힉스필드 MCP 연결 후 페이블이 직접 생성·검수·배선. 스타일은 실화면 대조로 v2 플랫 이모지로 전환[사용자 판정: 클레이 3D 괴리]. 빈상태 8 + 티어 7 완성·배선, 아이콘=Podo 유지, OG 보류(P1), empty-reminders 배선 P1. 상세·실측·사유는 docs/ILLUSTRATION_STYLE.md v2 + 자산 대장. 소요 ~5.3cr/잔여 ~4.7cr)

## W2-B: AI 아트 생성·검수·배선 (힉스필드 — 크레딧 확보됨)

- Severity/분류: 자산 제작(P0 아트) / 배정: 생성·검수=fable+사용자, 변환·배선=haiku/sonnet
- 필독: docs/ILLUSTRATION_STYLE.md **전체**(스타일 정본 — 프롬프트 카탈로그·검수 7항·파이프라인·수용 구조)
- 소유 파일: public/art/** (신규), 배선 대상은 ILLUSTRATION_STYLE 수용 구조 절의 명시 파일만

### 절차

1. 힉스필드 MCP 가용 확인(ToolSearch "higgsfield") — 없으면 프롬프트 팩을 사용자에게 출력(웹 생성) + 인테이크 폴더(`art-intake/`) 드롭 프로토콜.
2. 생성 순서: 빈상태 8종 → 티어 7종 → 앱 아이콘 → OG. (라벨 7종·스플래시는 P1 이월.)
3. 장당 검수 7항(ILLUSTRATION_STYLE) — 40px 축소 가독·grape-100 배경 정합·스타일 일관.
4. 후처리: 배경 제거 확인 → WebP q82 ≤60KB → `public/art/<이름>-v1.webp`.
5. 배선: EmptyState `art` prop 주석 해제 지점·TIER_ART 매핑. 아이콘이면 `scripts/gen-brand-assets.mjs` 재실행 + `public/sw.js` CACHE_VERSION 범프.
6. 자산 대장(docs/ILLUSTRATION_STYLE.md 하단 표): 파일·프롬프트·생성일·라이선스(Higgsfield 상업 이용 확인) 기록.

### 검증법: 브라우저에서 빈상태·와이너리 실렌더 스크린샷 + Lighthouse 이미지 경고 없음 + `npm run build`.

### 산출: 자산 파일 + diff + 검수 기록. 커밋은 게이트 후.
