상태: 완료 (2026-07-07 — 비네트 1회 생성 합격(0.15cr, 잔액 4.65), rembg 불필요 판명: 이미지 배경색을 캔버스색으로 통일해 이음새 제거, 타이포는 playwright HTML 렌더(MaruBuri Bold/SemiBold). 합성 소스 art-intake/og-compose.html 보존)

## P1: OG 이미지 — AI 배경 재시도 + 타이포 합성

- Severity/분류: 완성도(P0 이월 — 2회 구도 실패로 보류했던 건) / 배정: 상위 모델(생성 판단) + haiku(합성 스크립트)
- 필독: `docs/ILLUSTRATION_STYLE.md` v2 프리앰블·§E, 교체 규칙(같은 URL 교체 금지 예외 확인)
- 소유 파일: src/app/opengraph-image.png (교체), (합성 스크립트 신규 시) scripts/

### 배경 (자립 설명)

카톡 등 링크 미리보기용 OG 이미지는 현재 코드 생성본이 정상 동작 중. P0에서 z_image로 배경 2회 생성했으나 구도 실패(상하 흰 띠 / 보라 밴드+물방울 무늬) — "우측 2/3 빈 공간" 지시를 z_image가 잘 못 따름. 실패 프롬프트는 힉스필드 생성 이력(2026-07-06)에 있음.

### 스펙

1. 전략 변경: 배너 전체를 생성하지 말고 **정사각(1:1) 비네트 1장만 생성**(v2 프리앰블 + "cute flat vineyard vignette, grape trellis on a hill") → rembg 투명화(P0 파이프라인 재사용) → sharp로 1200×630 캔버스(#FBF7FE)에 좌측 배치 + 우측에 "포도알 — 한 알씩, 매일의 기록" 타이포(MaruBuri, `src/app/fonts/` 자체 호스팅 폰트 사용) 합성.
2. 생성 예산: 최대 3회(0.45cr). 3회 실패 시 중단·보고(현행 코드 OG 유지 — 실패해도 제품 무손상).
3. `src/app/opengraph-image.png` 같은 경로 교체 — OG는 SNS 크롤러가 읽는 것이라 sw.js 캐시 규칙 무관(교체 규칙의 예외임을 커밋 메시지에 명기).

### 검증법

- 로컬에서 파일 교체 후 1200×630 확인 + 텍스트 가독(축소 미리보기) + `npm run build`.
- 배포 후 카톡 미리보기 갱신은 크롤러 캐시라 지연될 수 있음(정상) — 기록만.

### 산출: 이미지 + 합성 스크립트 diff + 생성 이력(크레딧 소모) 기록. **커밋 금지**.
