상태: 완료

## W2-C: 전 기능 워크스루 (페이블 직접 — 전 라우트 실주행 검토)

- 방법: dev 서버(localhost) + playwright 실브라우저. 16개 라우트 전부 로드 + 콘솔/페이지 오류 수집 + 에러 배너 텍스트 검사 + 통계 3탭 전환·보드 생성 진입 스모크. 홈 제스처·보상 개봉·plant-gift·계정 관리는 W1 각 카드에서 이미 심층 검증됨.
- 결과: **16/16 라우트 정상 로드, 콘솔 오류 1건, 에러 배너 0건.**

### 발견 및 처리

| # | 발견 | 트리아지 | 처리 |
|---|------|---------|------|
| 1 | /messages에서 `EmojiIcon missing flat SVG for 🏋️ (1f3cb)` 콘솔 경고 + 404 — seed 샘플 메시지의 이모지가 fluent 자산 118종에 없음 | Low(위생) | 즉시 수정 — seed 이모지 💪(1f4aa, 자산 있음)로 교체. **교훈: CheerModal 등 이모지 선택지는 자산 보유 목록과 동기 필요** — 선택지가 자산에 없는 이모지를 노출하면 동일 증상 재발. 선택지 검사를 check-icons.mjs에 추가하는 것은 P1 후보 |
| 2 | /relay 이하 라우트 최초 로드 45~90초(dev Turbopack 온디맨드 컴파일 + 병렬 에이전트 부하) | 의도적 동작(dev 전용) | 프로드는 프리컴파일 — 조치 없음. 워크스루 시 대기시간만 참고 |

### 남긴 것 (P1 이월)

- 이모지 선택지 ↔ fluent 자산 동기 검사(check-icons 확장) — 카드화 대기.
- 실기기(안드로이드/iOS)에서 풀스와이프·long-press 감각 튜닝 — CDP 에뮬은 로직 검증까지, 물리 감각은 베타에서.
